import { TimeLengthDirective } from './time-length.directive';

describe('TimeLengthDirective', () => {
  it('should create an instance', () => {
    const directive = new TimeLengthDirective();
    expect(directive).toBeTruthy();
  });
});
